apiVersion: v1
kind: Service
metadata:
  name: {{ include "dremio-easy-chart.fullname" . }}
  labels:
    {{- include "dremio-easy-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dremio-easy-chart.selectorLabels" . | nindent 4 }}
